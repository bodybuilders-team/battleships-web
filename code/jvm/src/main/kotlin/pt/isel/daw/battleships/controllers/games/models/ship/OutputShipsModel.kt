package pt.isel.daw.battleships.controllers.games.models.ship

/**
 * Represents a ships list.
 *
 * @property ships the list of ships
 */
data class OutputShipsModel(val ships: List<OutputShipModel>)
